function mostrar()
{
	let numeroAlearotio;
	let maximo = 10;
	let minimo = 1;

	numeroAlearotio = Math.round(Math.random() * (maximo - minimo) + minimo);

	alert(numeroAlearotio);

}//FIN DE LA FUNCIÓN